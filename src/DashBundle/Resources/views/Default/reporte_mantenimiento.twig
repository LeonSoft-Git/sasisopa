{% extends('@Dash/layout.html.twig') %}

{% block body %}

    <div class="col-lg-12">
        <h2>Evaluación a servicios</h2>
        {% if app.user.level.idl == 1 or app.user.level.idl == 2 %}
            <a class="btn btn-default btn-cons m-t-10" href="{{ path('newrm') }}" role="button"><i class="fa fa-plus"> Insertar</i> </a>
        {% endif %}
        <hr>
        <div class="row">
            <div class="col-lg-12">
                <div class="table-responsive">
                    <table class="table table-bordered">

                        <tr align="center">
                            <td>No. Id</td>
                            <td>Nombre</td>
                            <td></td>
                            <td>link</td>


                        </tr>
                        {% for rep in reporte %}
                        <tr align="center">
                                <td>{{ rep.idrm }}</td>
                                <td>{{ rep.nombre }}</td>
                                <td><a href="{{ path('nwrn' , {'id': rep.idrm }) }}" data-toggle="tooltip" title="Ingresar recursos"><i class="fa fa-plus"></i></a></td>
                                <td><a href="{{ path('viewrm', {'id': rep.idrm}) }}" data-toggle="tooltip" title="Ver"> Ver Evaluacion {{ rep.idrm }} <i class="fa fa-eye"></i></a></td>
                        </tr>
                        {% endfor %}


                    </table>



                </div>


            </div>
        </div>
    </div>
{% endblock %}